# 1. Gzip Compression - Text files ko compress karne ke liye (Speed badhata hai)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/x-javascript application/json
</IfModule>

# 2. Browser Caching - Images aur CSS ko browser mein store karne ke liye
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# 3. React Routing Rules
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Sitemap aur Robots ko bypass kare
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^(sitemap\.xml|robots\.txt)$ - [L]

  # Baaki sab React index.html par bheje
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /index.html [L]
</IfModule>